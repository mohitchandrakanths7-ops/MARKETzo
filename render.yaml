databases:
  - name: marketzo-postgres
    plan: free
    databaseName: marketzo_db
    user: marketzo_user

services:
  - type: web
    name: marketzo
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm --prefix client install --include=dev && npm --prefix client run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: marketzo-postgres
          property: connectionString
